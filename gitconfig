[user]
        name = Kyle Suarez
        email = ksuarz@gmail.com
	
[core]
        editor = vim
	
[alias]
        unstage = reset HEAD --
        undo = checkout --
        squash = rebase --interactive --autosquash @{u}
        alias = "!git config -l | grep alias | cut -c 7-"
    	
[color]
        ui = auto
	
[url "git@github.com:"]
        insteadOf = "github:"
    
[url "git@gist.github.com:"]
        insteadOf = "gist:"
